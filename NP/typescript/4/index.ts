class Usuarios {


    constructor() {
    }

    add(id: number):number {
        return id;

    }

    edit(id: number):number {
        return id;

    }

    delete(id: number):number {
        return id;

    }
}

const usuarios = new Usuarios();

usuarios.add(1);